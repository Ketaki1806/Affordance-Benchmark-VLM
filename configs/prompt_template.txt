You are an affordance annotation expert. Given an image, generate action captions describing what a person can physically do with the visible object, not what the object is.

Object label: {object_label}

Rules:
1. Each caption must be an imperative action phrase: [Verb] the [visible part] to [short purpose/state].
2. Length: {min_chars}–{max_chars} characters (target ~{target_chars}). Count includes spaces and punctuation.
3. Word count: {min_words}–{max_words} words per caption.
4. Reference visible parts (cap, rim, handle, lid, drawer front, body, base, blade, etc.).
5. most_probable: exactly {num_most_probable} physically valid affordance for this object state.
6. negative: exactly {num_negative} hard negative: same object/part, plausible wording, wrong affordance (e.g. open vs refill, pull vs push). NOT unrelated actions.
7. Positive and negative must be similar length (within {max_length_delta} characters).

Return ONLY valid JSON (no markdown):
{{
  "most_probable": ["one valid affordance caption"],
  "negative": ["one hard negative caption"]
}}

Examples:
- Positive: "Twist the cap to open the bottle."
- Hard negative: "Twist the cap to refill the bottle."

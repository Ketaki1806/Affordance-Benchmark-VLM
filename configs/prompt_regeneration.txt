The negative caption below was rejected by a CLIP adversarial filter. Generate ONE harder replacement.

Object label: {object_label}

Valid positive caption:
{positive_list}

Rejected attempts (caption | reason | sim_pos | sim_neg | gap):
{rejection_details}

Rules:
- {min_chars}–{max_chars} characters, {min_words}–{max_words} words.
- Same visible object/part as the image, plausible wording, but WRONG affordance.
- Must be harder than rejected captions: closer wording to the positive, same length band.
- If reason was clip_prefers_negative: use same verb/part as the positive but wrong action.
- If reason was too_easy_large_gap: stay same object/part; make affordance more confusable.

Return ONLY valid JSON:
{{
  "negative": ["single hard negative caption"]
}}

REGENERATION_TEMPLATE = """The following negative affordance captions were too easy for a vision model to distinguish from the image (semantically too distant or obviously wrong).

Object label: {object_label}

Rejected negatives:
{rejected_list}

Generate {num_negative} NEW hard negative captions only.
Rules:
- {min_chars}–{max_chars} characters, {min_words}–{max_words} words each.
- Same object/part as the image, plausible but WRONG affordance.
- Must be harder: visually and semantically closer to valid actions than the rejected ones.
- Similar length to these valid positives:
{positive_list}

Return ONLY valid JSON:
{{
  "negative": ["caption1", "caption2"]
}}
"""

name: affordance_benchmark
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch
  - pytorch-cuda=12.1
  - pip
  - pip:
      - transformers>=4.46.0
      - accelerate>=0.34.0
      - huggingface_hub>=0.26.0
      - sentencepiece>=0.2.0
      - safetensors>=0.4.0
